---
import ImageLazy from "@components/extras/ImageLazy.astro";

const {
  animationdirection = "fade-right",
  textColor = "[--secundary-color]",
  fontSize = "",
  fontSizeSlogan = "",
  isMobile = false,
  isFooter = false
} = Astro.props;
---

{
  isMobile ? (
    <a
      data-aos={animationdirection}
      href="/"
      class={`flex  w-fit flex-col pt-1 font-normal text-${textColor} relative w-[20%] min-h-[95px] ${!isFooter ? "min-w-[45%] md:min-w-[35%]" : "min-w-[75%] md:min-w-[100%]"} ` }
    >
        <ImageLazy
            src="/img/logo/nearShoringLogoBlack.svg"
            width="200"
            height="114"
            className="pt-12 relative pt-[25%] sm:pt-[32%] relative"
            classNameImage="logo__img--mobile"
            slot="image"
          />
    </a>
  ) : (
    <a data-aos={animationdirection} href="/" class={`logo hidden md:flex flex-col w-[70%] max-w-[50rem] 2xl:max-w-[55rem]`}>
      <ImageLazy
            src="/img/logo/nearShoringLogoBlack.svg"
            width="200"
            height="114"
            className="pt-[30%] relative "
            classNameImage="h-full logo__img"
            slot="image"
          />
    </a>
  )
}